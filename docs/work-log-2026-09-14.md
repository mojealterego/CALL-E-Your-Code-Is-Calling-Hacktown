# AegisFleet — 2026-09-14 engineering work log

## Completed in this session

- Hardened structured CALL-E result validation:
  - `task_completed` is now strictly boolean when present;
  - appointment evidence arrays reject malformed/non-string/blank entries instead of silently filtering them.
- Hardened appointment reconciliation:
  - first-visit instructions cannot be reported before positive patient identity;
  - first-visit instructions cannot be reported for a non-first visit;
  - first-visit confirmation requires all applicable administrative instructions.
- Hardened route reconciliation:
  - CALL-E `escalation_needed` is carried into the transaction evidence;
  - anything other than explicit `none` blocks route commit and enters recovery;
  - missing escalation status is therefore not treated as safe.
- Hardened transaction preparation in the orchestrator:
  - invalid prepared constraints are terminalized as `escalated` instead of leaving a `validated` record stranded;
  - transaction IDs now include participant identity to avoid collisions across vehicles sharing an incident ID.
- Hardened audit ledger integrity:
  - public snapshots/history/reservations return deep copies;
  - stored audit events are detached from mutable caller-owned nested values;
  - legacy `complete()` now follows `calling → verifying → resolved` when the completion conditions are satisfied.
- Hardened the controlled live workflow:
  - unique `AEGIS_LIVE_TEST_ID=${{ github.run_id }}`;
  - explicit production CALL-E endpoint;
  - Polish `PL` / `pl-PL` defaults;
  - configuration validation;
  - authentication preflight;
  - regression tests and typecheck before the single live execution step.
- Added regression coverage for structured-result validation, escalation blocking, appointment evidence invariants, audit immutability and FSM completion compatibility.

## Files changed

- `src/validation.ts`
- `src/transaction.ts`
- `src/orchestrator.ts`
- `src/ledger.ts`
- `.github/workflows/live-call.yml`
- `tests/validation-hardening.test.ts`
- `tests/appointment-transaction.test.ts`
- `tests/transaction.test.ts`
- `tests/ledger.test.ts`

## Commits

- `0cf6fa971740bdcd5a3cf7c0a086451d9ebe3b76` — strict structured-result validation
- `2324a0d89004123f9deed91a753b80aab3976489` — validation regression tests
- `fa4e0de54c2ba8c9fb82ee426d2654717c61d3b2` — appointment evidence invariants
- `396b59e3fc06a2bf41df4d55c76b71cfa8593129` — appointment reconciliation tests
- `ac43eea392e18e1c35b600832fd920943f72d72e` — preparation/transaction identity/escalation evidence plumbing
- `738c6cc225ce864d65c1399dde58fd7e9bebd802` — escalation commit guard
- `afcd5fdfb45b6f3c99a547f48b671a9cf7765c2f` — route reconciliation tests
- `f0478fc1766ca0c0680a98b8891bb4d1e412826f` — audit immutability + FSM completion compatibility
- `7f66106551476eed8eb1d4ffb00ff1d2e54c9f04` — ledger regression tests

## Verification

GitHub Actions CI run `34830164178` for commit `396b59e3fc06a2bf41df4d55c76b71cfa8593129` completed successfully. It ran `npm install`, `npm test`, `npm run typecheck`, and `npm run demo`.

The subsequent commits were pushed after that successful run; a fresh CI run for the current head was observed as `in_progress` at the time this log was written and must be allowed to complete before claiming final green status.

## Live status

No live call was initiated by these engineering changes. The last real live attempt before this session failed with CALL-E `Insufficient CALL-E balance`; the application correctly recorded `recovering` and `callsPlaced: 0`. The user has since replaced `CALLE_API_KEY` in GitHub Secrets.

The final demo is **not recorded**. The next external action remains exactly one explicit live-workflow test using the new key. Only a verified `callId` + successful terminal result + `resolved` AegisFleet state permits proceeding to the final demo recording.

## Known remaining hardening item

The route transaction still represents ETA as time-of-day only. A fully general cross-midnight ETA proof requires an absolute ETA date/time (and timezone) in the transaction contract. This does not affect the current Polish appointment live path, but it remains a correctness hardening item for generic route transactions.

## Content lock

The approved Polish receptionist dialogue in `src/calle.ts` was not rewritten or replaced during this work.
