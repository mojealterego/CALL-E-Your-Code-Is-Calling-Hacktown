# AegisFleet — 2026-09-14 engineering work log

## Completed in this session

- Hardened structured CALL-E result validation: strict boolean `task_completed`; malformed appointment evidence is rejected instead of silently filtered.
- Hardened appointment reconciliation: identity-gated first-visit instructions, rejection of first-visit instructions for non-first visits, and complete first-visit administrative evidence requirements.
- Hardened route reconciliation: `escalation_needed` is carried into the transaction evidence; anything other than explicit `none` blocks route commit and enters recovery; missing escalation status is unsafe.
- Hardened transaction preparation: invalid prepared constraints are terminalized as `escalated` rather than stranding a `validated` ledger record; transaction IDs include participant identity.
- Hardened audit ledger integrity: public reads return deep copies; stored nested audit data is detached from caller-owned objects; legacy `complete()` follows `calling → verifying → resolved` for a verified completion.
- Hardened the controlled live workflow with run-scoped idempotency, explicit production CALL-E endpoint, Polish `PL` / `pl-PL`, secret/phone validation, authentication preflight, regression tests and typecheck before the live execution step.
- Added regression coverage for structured-result validation, escalation blocking, appointment evidence invariants, audit immutability and FSM completion compatibility.
- Restored the canonical CALL-E instruction body after removing two previously added, unauthorized opening-control lines.
- Locked six user-specified appointment spoken phrases with explicit `say exactly` instructions in `src/calle.ts`; no additional provider-native greeting control was invented.
- Added the reusable `voice-transaction-coordinator` community contribution package to the user's fork `mojealterego/awesome-phone-call-agents` on branch `feat/voice-transaction-coordinator`, including `SKILL.md` and offline examples.

## Files changed

Core changes include `src/validation.ts`, `src/transaction.ts`, `src/orchestrator.ts`, `src/ledger.ts`, `src/recovery.ts`, `.github/workflows/live-call.yml`, `tests/validation-hardening.test.ts`, `tests/appointment-transaction.test.ts`, `tests/transaction.test.ts`, `tests/ledger.test.ts`, and `src/calle.ts`. Documentation and submission files were also added/updated throughout the repository.

## Verification

- Verified latest CI run `34859717098`, workflow `CI`, run #519, head `a659761c740aecb22204c20b7a802737fa935fde`: **success**.
- The latest CI job completed `npm install`, `npm test`, `npm run typecheck`, and `npm run demo` successfully.
- The verified CI head includes the canonical locale restoration and appointment wording correction.

## Live-call state

No live call was initiated by these engineering changes. The last real live attempt before this session failed with CALL-E `Insufficient CALL-E balance`; the application correctly recorded `recovering` and `callsPlaced: 0`. The user has replaced `CALLE_API_KEY` in GitHub Secrets.

The final demo remains **NOT RECORDED**. The next external action is exactly one explicit live-workflow test. Only a verified CALL-E `callId`, successful terminal result, and AegisFleet `resolved` state permit the final demo recording.

## Community submission state

The user fork `mojealterego/awesome-phone-call-agents` now contains the contribution on branch `feat/voice-transaction-coordinator`. The upstream repository is `CALLE-AI/awesome-phone-call-agents`. The connected GitHub integration has pull access to upstream but cannot create the cross-repository PR directly; therefore no upstream PR URL is claimed here.

## Known remaining hardening item

Generic route transactions still represent ETA as time-of-day only. A fully general cross-midnight ETA proof requires an absolute ETA date/time and timezone in the transaction contract. This does not affect the current Polish appointment live path.

## Content lock

The canonical CALL-E instructions remain the source of truth. The six spoken phrases explicitly corrected by the user are now locked with `say exactly` in `src/calle.ts`. No other spoken wording has been intentionally changed in the latest correction.
