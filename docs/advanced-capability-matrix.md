# Advanced Cognitive Capability Matrix

This matrix records the requested concepts and their bounded implementation status in AegisFleet. Research names are architectural references, not claims of reproducing proprietary models or papers.

| Requested capability | Bounded implementation |
|---|---|
| Bitemporal / CoALA / G-memory / Holographic / Shimi / MCP | Existing bounded memory, graph, index and gateway layers |
| AlphaEvolve / DGM / digital genotype / mutation / RSI | Gated candidate evolution and explicit rollback pipeline |
| R2 / R3 / Reflexion / SNN / Zenoh | Deterministic reasoning, retrospective loop, Rust LIF and isolated transport |
| JEPA / GoT / adversarial / CEV / GCP / Gödel / ImandraX / SEGPA / OESI | Bounded predictive, graph, adversarial, constitutional and invariant gates |
| RAG 2.0 / episodic / working / procedural / retrospective / counterfactual | Temporal retrieval and bounded reasoning contracts |
| 34–40 genetic evolution through recursive state | `src/advanced-cognitive-stack.ts`, `src/research-adapters.ts`, `src/imandrax-mars.ts` |
| 41–69 Meta-Architect skills | `src/meta-architecture-skills.ts` and associated tests |
| 70 Active Inference / Free Energy | `activeInference()` prediction-error/free-energy gate |
| 71 Topological Concept Manifolds | `topologicalConceptManifold()` bounded proximity graph, components and void signal |
| 72 Gödelian Anti-Paradox Gating | `godelianGate()` fail-closed invariant budget and checks |
| 73 Polysemantic Disentanglement | `polysemanticDisentangle()` weighted feature attribution boundary |
| 74 Semantic Entanglement | `semanticEntangle()` explicit relation propagation |
| 75 Fractal Swarm Instantiation | `fractalSwarm()` bounded recursive fan-out |
| 76 Autopoietic Self-Maintenance | `autopoieticBoundary()` protected core/external capability separation |
| 77 Zero-Shot Cross-Modal Synesthesia | `synestheticTransfer()` deterministic cross-modal signal boundary |
| 78 Zero-Knowledge Cognitive Proof | `zkCognitiveProof()` commitment verification without witness disclosure; not a zk-SNARK implementation |
| 79 Ontological Simulated Annealing | `ontologicalAnnealing()` bounded deterministic exploration perturbation |
| 80 Anticipatory Pre-Cognitive Rendering | `anticipatoryRender()` bounded future scenario/solution cache |
| 81 HDC / VSA | `vsaBind()` and `vsaBundle()` binary hypervector operations |
| 82 QPU Orchestration | `compileQASM()` sanitized QASM boundary; simulator/external backend selection only |
| 83 Causal Emergence | `causalEmergence()` explicit agent-to-macro-agent compression |
| 84 Nested World Models | `nestedWorldSimulation()` bounded baseline/candidate simulation comparison |
| 85 Hardware Agnosticism / FPGA | `hardwareCompile()` target IR; live hardware programming is prohibited and human approval is mandatory |
| 86 NARS | `narsRank()` frequency × confidence belief ranking |
| 87 CEV | `cevGate()` explicit constitutional predicates |
| 88 Temporal Inversion Routing | `temporalInversionCorrection()` historical causal-parent correction record; history is immutable |

## RSI safety boundary

The system may generate and benchmark candidates, but no research module may autonomously modify source, credentials, policy, phone authorization, production infrastructure or audit history. Release remains:

`OBSERVE → PROFILE → HYPOTHESIS → GENERATE → AIR-GAPPED SANDBOX → RED TEAM → FORMAL GATE → BENCHMARK → SHADOW → HUMAN/POLICY APPROVAL → ATOMIC RELEASE → ROLLBACK`

Quantum, FPGA, ZK, TDA, active-inference and hardware paths are bounded integration contracts until a separately authorized implementation is supplied and verified.
